# 更新记录

本技能为自有技能，不连远程仓库、没有自动更新；改动直接改本目录并在这里补一条。

## 1.8.0 — 2026-09-25

- 写稿工序升级，方向定死「传播靠稿子，稿子靠思考和利益」：
  - 动笔前先憋 **3 条候选判断**竞争（每条带因果链），选最尖的一条当全片脊柱，一条片只讲一个判断；不许复述通稿信息。
  - **先定金句再倒着写**：金句必须是观众愿意原样转给朋友的一句话；开头三句必须点到观众利益（钱包、饭碗、安全、孩子之一）。
  - 交稿前过**利益三问**（观众看完得到什么 / 会转给谁 / 凭什么转），答不上打回重磨；与原套路四问并列为两道闸。
- 「交付前自查」清单同步增加利益三问与单一判断检查项。

## 1.7.0 — 2026-09-24

- 技能只负责在用户电脑上制作、校验和打包 ZIP；移除本地 MCP 授权、自动上传、收件地址和手机接力说明。
- 交付改为：用户在自己的电脑浏览器打开 GetCut，登录后选择 ZIP，确认文案与设置并开始生成。
- 明确不读取 Cookie、密码、令牌或 API key，不代替用户登录、上传、建任务或触发云端渲染。

## 1.6.0 — 2026-09-24

- Skill 仓库新增自包含的 `getcut_mcp` 本地 stdio MCP 运行包，不依赖开发者电脑上的 VideoGen 源码。
- 新增 `scripts/bootstrap_mcp.py`：首次运行自动检查 Python、创建 `%USERPROFILE%\\.getcut\\mcp\\venv`、安装 MCP、运行真实握手并确认四个工具。
- bootstrap 只准备本地 MCP，不读取 Cookie、不索要密码、不自动授权、不自动上传；已运行的 Agent 需要重开后才能重新加载工具。
- `SKILL.md` 移除对 Skill 外部 `docs/getcut-mcp.md` 的依赖，明确 `restart_required` 和不支持本地 stdio MCP 的处理方式。

## 1.2.0 — 2026-09-23

- 投喂改「先领收件地址，再 PUT」：匿名 `POST /api/intake` 已关闭（单包 512MB ×
  一小时 10 次 = 陌生人一小时能塞 5GB，服务器那块盘只剩十几 G；而且免登录能用
  就意味着客户端得拿站点主钥匙，那把钥匙能看见全站任何客户的片子）。
  现在三条路按顺位：配了 GetCut MCP 就调 `feed_bundle`；有内置浏览器就在已登录
  页面 `fetch('/api/intake/ticket')` 拿 `put_url`；都没有就让用户在网页上生成一条
  收件地址粘回来。地址一次一用、24 小时作废、单包上限 200MB。
- 落地页不再显示选题标题与口播开头：那条 `/i/<码>` 天生会被转发，内容只给登录后
  导入的本账号看。截图验收点改成认「包先到了一步」这张页。
- MCP 的 `GETCUT_TOKEN` 从此填**这个账号自己的设备令牌**（「登录设备」里可单独踢掉），
  不再填服务器 .env 里那把主钥匙。

## 1.0.0 — 2026-09-19

- 第一版：素材包协议 v1（content.json / manifest.json / images / videos），
  含五连问流程、钩子标题规则、ffmpeg 选段规范、validate_bundle.py 自校验与打包指引。

## 1.1.0 — 2026-09-19

- 交付升级：打包完不再让用户手动拖 zip。新增「自动投喂」流程——POST /api/intake
  上传拿 `/i/<码>` 短链，浏览器打开即见「素材包已送达」落地页，点「确定，导入工作台」
  自动导入并建好项目，再点「确认内容并开始生成」才正式渲染。
- 浏览器调用规则：宿主有内置浏览器（如 browser-use）就用内置并截图验收；没有就
  cmd/open/xdg-open 唤起系统默认浏览器；手机端直接递短链。
- 落地页带手机接力二维码（24 小时有效、导入一次即收走）；没登录先登录，回来自动续接导入。
- 手动拖 zip 保留为投喂失败时的后备路径。

## 1.2.0 — 2026-09-19

- 对话剧本正式定稿（替换原「第零步五道题」）：第 1 轮甩 5 条热点候选（四体裁：
  快讯/深度观察/锐评/数字盘点，时长绑体裁）→ 第 2 轮交稿（五道工序：备料/定脊柱/
  填骨架/磨句子/筛，交稿带脊柱+体裁+2 标题候选）→ 第 3 轮只问形态 → 第 4 轮打包投喂。
- 交付新增 MCP 首选路径：宿主配了 getcut MCP（feed_bundle/submit_render/job_status/
  wait_for_job/download_video/list_voices/list_projects）即全程接口化，不开浏览器；
  intake 短链降为次选，手动拖兜底。

## 1.3.0 — 2026-09-22

- 取材环节改成「槽位表 + 一次并发抓」：新增 `scripts/fetch_assets.py`，素材包目录里先填
  `slots.json`（每格 num / anchor / summary / tags / queries / site），脚本按格并发抓取。
  四种取法：`pexels`（图库 API，概念图主力）、`mixkit`（B-roll 主力，先落 24 小时索引再按
  ID 下载）、`og`（抓网页 og:image，拿公司/机构/产品真图）、`bing`（兜底）。
- 脚本自带护栏：图片 12 路 / 视频 4 路并发；对同一站点按时间窗限速（防把用户 IP 送进黑名单）；
  入库前验魔数与真实宽高、按 md5 去重；视频自动裁成 9 秒单镜头；回执落 `registry.json`，
  并拼一张 `contact_sheet.jpg` 供一次目检。
- 返工从「整轮重跑」降为「只补那几格」：`--only 14,25` 补单格、`--report` 看缺格。
- key 规矩写死：图库 API 用用户自己申请的 key（环境变量或 `--pexels-key`），skill 与素材包
  里一律不带 key；没 key 时自动降级到搜索引擎并明确提示质量下降。
- 新增 `scripts/build_manifest.py`：`manifest.json` 由 `registry.json` 生成，不再手抄；
  顺序按槽位编号走，`source_url` 与 `license` 自动带上。
- 文档同步：第二步整节重写（含「queries 只写能被字面搜到的东西」这条硬规矩）、第三步注明
  脚本已代劳裁切、第四步改为跑生成脚本、工作流与交付前自查各补两条。
- 中间产物（`slots.json` / `registry.json` / `contact_sheet.jpg` / `.tmp/`）明确不进 zip。
- 自测后补的三处：`og` 槽位支持直接填图片地址（官网首页常只有 208×32 的小 logo，真图在
  新闻页里），并给品牌格单独放宽尺寸下限；某格第一个候选验货不合格时改为**换下一个候选**
  而不是判死整格；填不上的提示按取法分别给出下一步（og 提示换页面地址，搜索提示换具体名词）。
- 索引文件改原子写入 + 进程内只建一次，避免两个视频槽位并发把索引写坏。
