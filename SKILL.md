---
name: getcut-bundle
description: 把任意内容（PDF、论文、网页、聊天记录、已写好的口播、热点选题）整理成可上传 GetCut 成片台的短视频素材包 zip：与用户定稿口播和钩子标题、下载真实图片与 B-roll、用 ffmpeg 裁选段、写 content.json 与 manifest.json、自校验后打包。需要能下载文件到本地磁盘、shell 里有 ffmpeg 和 ffprobe、能打 zip。当用户想把素材或选题做成短视频/成片，或要收集与口播配套的画面素材时使用。
---

# GetCut 短视频素材包

把用户给的任意内容，整理成一个可以上传到 GetCut 工作台渲染成片的「素材包」。

输入不限对话。PDF、论文、网页链接、Word、聊天记录、已写好的口播——只要用户能丢进当前 Agent，你就先读材料，再做成素材包。zip 不是成片，是 GetCut 渲染管线的输入。

## 先确认你能不能做

这个任务需要以下三项能力，缺任何一项都要立刻告诉用户，不要硬着头皮往下做：

1. 能联网访问网页并**下载图片、视频文件到本地**（不是只给链接）。
2. 能执行 shell 命令，环境里有 `ffmpeg` 和 `ffprobe`。
3. 能把本地目录打包成 zip 并把文件交付给用户。

如果你只能上网搜索、不能下载文件（例如大多数手机端 AI 助手），直接说明，让用户换一个工具。

## 关于这份技能本身

这是自有技能：不连任何远程仓库、没有自动更新，也不需要开工前的版本检查。GetCut 服务端协议有升级时，直接同步改本目录的 `references/` 和 `scripts/validate_bundle.py`，并在 `CHANGELOG.md` 补一条。

## 工作流

1. 按「第零步」的对话剧本走：选题候选 → 交稿 → 问形态，**稿子被用户点头之前不找素材**。
2. 定稿口播文案，竖屏信息版连钩子标题一起定稿。
3. 按口播填 `slots.json` 槽位表，跑 `scripts/fetch_assets.py` 一次并发抓完；看 `contact_sheet.jpg` 目检，**只补不合格的那几格**。
4. 只有手工塞进 `videos/` 的原片才需要定选段——脚本抓的视频已自动裁成 9 秒。
5. 跑 `scripts/build_manifest.py` 从回执生成 `manifest.json`；字段规矩见 `references/manifest-schema.md`。
6. 写 `content.json` 前先读 `references/content-schema.md`。
7. 跑 `scripts/validate_bundle.py` 自校验，通过后再打包。

## 交付物

一个 zip，解压后结构如下：

```
content.json
manifest.json
images/
  01-xxx.jpg
  02-xxx.png
videos/
  01-xxx.mp4
```

`images/` 和 `videos/` 的文件名随意，但必须与 `manifest.json` 里的 `file` 字段一致。

`slots.json`、`registry.json`、`contact_sheet.jpg`、`.tmp/` 是干活的中间产物，**不进 zip**（下面第六步的打包命令已经把它们排除了）。

你只负责**内容**：文案、素材、多音字读音、钩子标题。成片形态、音色、语速、字幕开关、IP 形象、背景音乐这些渲染参数由用户在 GetCut 工作台里渲染前自己选，不要写进 `content.json`。

## 第零步：对话剧本——先选题，再交稿，别用问卷轰炸

用户开了个头（「来一篇 XX 热点」「帮我把这份材料做成视频」）后，按下面的剧本走。原则：**给选项代替提问题**，每一轮都把默认值明说出来，不闷头做主。

### 第 1 轮：甩 5 条热点候选（用户直接点题则跳过这轮）

用户没指定选题时，先搜当天热点（按用户的话头抓科技/政治/经济榜），挑 5 条候选一次递出。每条四件套：

- **这事是什么**：一句话讲清事件；
- **为什么能打**：冲突在哪 + 有哪些硬数字（拿不出数字的选题不做）；
- **评分**：按下面的六维打分卡给分（各 10 分，满分 60），一行写完，如「热度9 时效8 冲突7 关联8 情绪7 有料9 → 48」，后面跟一句「料在哪」——那条别人没说过的话初步是什么。写不出「料在哪」的视为有料不达标；
- **建议体裁**：默认**深度观察（3 到 5 分钟）**——产品定调是深度稿子，不是新闻稿。四类体裁——
  - **深度观察**（3 到 5 分钟，默认）：讲背后逻辑，即用户说的「有内涵不浮于表面」；
  - **锐评**（3 到 4 分钟）：态度先行，观点鲜明；
  - **数字盘点**（2 到 4 分钟）：榜单对比，一串数字讲完；
  - **快讯**（60 秒内）：只在天窗期抢首发用——事件刚炸出来、深度稿还没长出来时先出一条，有料了再出深度版。

六维打分卡：

1. **热度**：多少人在讨论，有没有上热搜；
2. **时效**：刚炸出来多久，天窗期还剩几小时；
3. **冲突**：有没有「说一套做一套」「不该发生却发生了」的拧巴；
4. **关联**：能不能点到观众的钱包、饭碗、安全、孩子；
5. **情绪**：看的人会冒什么情绪——愤怒、惊讶还是焦虑；
6. **有料**：能不能找到「两条消息摆在一起才看出的门道」，或这个圈子的老手才懂的门道。**这一维一票否决：再热的热点，挖不出料就不做**——做了也是新闻复读，宁可今天不出片。

第 1 条标「推荐」，用户回个编号即可，可附带必讲点。用户自己出题或甩材料 → 直接进第 2 轮。

### 第 2 轮：交稿（用户甩了成品稿则跳过，一字不改只查读音）

写稿是自己闷头干的活。**用户看到的永远只有三样：口播稿正文、这条片观众记住的一句话、两个标题候选。**用户要做的选择就一个——回「就这版」，或者说哪里要改。不要对用户讲方法论，不要甩黑话（「脊柱」「骨架」这类词只许自己心里有数，对用户就说「这条片观众记住的一句话」）。

自己写稿按这六步（心里有数就行，别讲给用户听）：

1. **先查资料**：把事情的时间线、各方原话、能上卡的数字查齐，时间线排成一列（下一步找料全靠它对日子）。查不齐不动笔。
2. **先找料，不找到不动笔**：料只有两种——（a）**两条消息摆在一起才看出的门道**：同一天的两件事、原话对上日期、承诺对上事实，摆在一起撞出来的那句话；（b）**这个圈子的老手看到新闻会立刻想到、但通稿不会写的门道**。两种各写出一句实在的话。一句都写不出来，回第 1 步接着查接着想；真没有，告诉用户这个题今天做不了——**新闻复读机没人转，宁可不出片。**
3. **三行字定切入点**：动笔前写死三行——（一）这条片拍给谁，具体到「刷到它的人昨天在烦什么」；（二）他心里本来就有的那个疑问或火（人群里已经存在，你只是命中，不用制造）；（三）我们给他的那个**别处没有的答案**，一句话。三行写不出来，说明料没找透，回第 2 步。验法：把第三行讲给一个完全不知道这事的人听，他会追问「后来呢」——不追问就换门再试。
4. **按五段骨架写**：全片一条链子走到底，每句话勾着下一句。不搞「每 15 秒换一个刺激」的串珠——那是 1 分钟新闻稿的写法，撑不住 3 分钟。
   - **钉疑问**（前 20 秒）：第一句抓人（禁「大家好」式开头），三句话内把观众心里那个拧巴直接说穿，先不回答，让他带着问题往下走。开头三句必须点到观众自己的利益（钱包、饭碗、安全、孩子，占一个就够），点不到就重写开头。
   - **排错误答案**（约 1 分钟）：把大众最容易想到的两三个解释摆出来，各用一条事实打掉。这段是深度的体感——观众觉得你在替他认真想，而不是在念结论。不许空口说「其实不是」，每打一个都要亮事实。
   - **亮证据**（1 到 2 分钟）：把第 2 步找的料一步步摆给观众——先摆消息 A，再摆消息 B，让证据自己撞出结论。按时间线推进，一环勾一环，这句为下一句埋东西。
   - **给答案**（约 30 秒）：三行字的第三行，一句说穿，再补两句坐实。这就是「观众记住的那句话」，必须含这条片特有的事实或数字——能套任何片子的对仗句是套话，不算金句。
   - **落到观众**（30 秒到 1 分钟）：这个答案改变了什么——对他钱包饭碗安全孩子意味着什么，接下来该盯哪条线。开头钉的疑问在这里正式接住。
5. **写完念一遍**：句子短（每句 ≤20 字）、数字说人话（1PB=26 万部电影）、避开配音读错的坑（数字写汉字，「960」口播写「九六零」，标题保留数字）。顺手掐空话：每个抽象断言（「竞争激烈」「影响深远」）要么删掉，要么兑换成同量级的具体对比——五十万张卡该跟「英伟达最大的集群才多少张」比，不是「像一座工厂」。
6. **过审再交**：两三版各过三道闸。**必看三问**——前 3 秒把观众的疑问钉出来了吗？开头三句点到观众利益了吗？开头钉的问题结尾接住了吗？**有货三问**——片子里有没有至少一处观众别处看不到的料？把中心判断讲给不懂行的人，他会追问「后来呢」吗？通稿复述有没有超过全片一半？**不庸俗清单**——有没有「细思极恐」「你敢信」「水很深」这类惊悚口癖？有没有开头承诺、结尾没接住的标题党？结尾有没有突然上价值的鸡汤？情绪是从事实的摆法里出来的，还是靠形容词喊出来的——**让数字和事实表演，别让形容词表演。**再加利益三问（观众看完得到什么 / 会转给谁 / 凭什么转），答不上来的那版打回重磨；都过关的版本里挑最尖的递出去。

交稿时给用户的就是那三样，外加体裁和时长（不用问——绑死体裁，默认深度观察 3 到 5 分钟，换算表见第一步）。用户改或回「就这版」再往下走。

### 第 3 轮：只问一件事——形态

「形态还按竖屏信息版吗？」（回「默认」即可）。竖屏信息版收横构图素材并必写 hook_headline；竖屏/横屏视频反过来一律不写。用户没回答 → 按竖屏信息版兜底，并在回复里明说。

### 第 4 轮：配素材 → 校验 → 打包 → 交给用户上传

按定稿收素材（第二步、第三步的规矩不变），校验打包后走「交付」章节。这个技能只负责把 ZIP 做在用户电脑上，不负责登录 GetCut、不读取浏览器凭据、不自动上传，也不在服务器上创建任务。用户拿到 ZIP 后，在自己的电脑浏览器打开 GetCut，登录并手动选择这个 ZIP，确认文案和设置后开始生成。
### 特殊情况

- 用户甩成品稿 → 尊重作者一字不改，只查读音坑 + 配素材 + 打包，从第 3 轮起；
- 用户直接点题 → 跳第 1 轮；
- 用户说「你定」→ 按推荐走，但每个默认值都要在回复里明说，不声不响做主等于没做。

## 第一步：确认口播文案

`content.json` 里的 `script` 是一段**定稿的口播文案**：它会被逐字朗读成配音、逐字显示成字幕，GetCut 不会再改写、润色或增删一个字。所以在动手找素材之前，先把文案定下来。

- 如果对话里已经有一份用户确认过的定稿，直接用它。
- 如果还没定稿，先别自己拍板。第零步那一轮已经把时长、受众、风格、必讲信息点一起问过了，按用户的回答写草稿，用户改完并明确说「就这版」之后再继续。
- 文案该怎么写、什么风格更好，按你和用户聊出来的结论来，这份技能不替你决定。

唯一的硬要求：最终放进 `script` 的必须是能直接逐字朗读的成品正文，不要标题、分段小标、镜头说明、emoji 或者括号里的舞台提示。

### 时长与字数

成片时长完全由配音朗读 `script` 的实际长度决定，没有别的旋钮：GetCut 不会为了凑时长增删一个字，也不会拉长或压缩画面来对齐目标秒数。所以**字数写多少，片子就多长**。

中文口播原速约**每秒 6 字**，按这个速率换算目标时长：

- 1 分钟（快讯）≈ 360 到 380 字
- 3 分钟 ≈ 1080 字
- 4 分钟 ≈ 1440 字
- 5 分钟 ≈ 1800 字

产品定调是**深度稿子，不是新闻稿**：默认做 3 到 5 分钟的深度观察，把一个事讲透；只有事件刚炸出来、料还没长出来的天窗期才出 1 分钟快讯抢首发。3 分钟以上的片子靠「一条链子走到底」的结构留住人，不能靠刺激点串珠硬撑。

### 多音字读音

文案定稿后，扫一遍里面的多音字，把配音容易读错的写进 `content.json` 的 `pronunciations`。比如「调用量」的「调」该读 diào，配音默认会读成 tiáo。写法见 `references/content-schema.md`。

只标真正会读错的那个字，标得越少配音的语气越自然，字幕时间戳也越准，所以别整句都注音。

### 钩子标题

**竖屏信息版必写，另两种形态一律不写。** 顶部标题带上那条**钩子大标题**，从口播文案里提炼，写进 `content.json` 的 `hook_headline`。它不进配音、不进字幕，只负责开场抓注意力。

- 推荐 2 行：第一行铺垫，第二行给结果或冲突。
- 克制但有钩：用具体事实或数字抓人，不用「细思极恐」「你敢信」这类惊悚口癖——标题惊悚，片子就庸俗。
- 每行不超过 12 个中文字，把最有冲击力的数字或结论用 `[[ ]]` 圈出来做高亮。英文与数字只占半个字宽，`DeepSeek砸1.4亿` 这样的行折合 8.2 个字，不算超。超过 12 会告警，超过 14 会校验失败。
- 跟文案一起给用户确认，别自己拍板。给 1 到 2 个候选让用户挑或改，用户点头后再往下走。
- 另两种形态没有这块版面，写了也不会出现在画面上，还会把 GetCut 渲染界面的形态初值拧成竖屏信息版，跟用户选的形态冲突，所以一律省略此字段。

示例：

```json
"hook_headline": {
  "lines": ["DeepSeek砸1.4亿", "一锁就是[[36个月]]"]
}
```

写法与校验规则见 `references/content-schema.md` 的 `hook_headline` 小节。

### 资料来源

文案定稿后，把引用过的公开资料名称写进 `content.json` 的 `sources`。它不进配音、不进字幕，只渲染为画面底部的小字。

**默认都写上，不用问用户要不要**。写文案时你查过哪些资料，你自己最清楚；露不露是用户在 GetCut 渲染界面上的开关，不是你要替他决定的事。

- 有引用就写，一条都没有就省略这个字段，别为了凑数编来源。
- 只写来源名称（公告名、媒体报道名等），不要写 URL，单条不超过 24 个中文字宽。
- 不要写「资料来源：」前缀，那个由 GetCut 拼。
- 免责声明是另一回事：它的开关和正文都在用户那边的渲染参数里，跟 `sources` 各自独立，不要写进 `content.json`。

示例：

```json
"sources": ["宇树科技招股书", "DeepSeek 官方公告", "新浪财经"]
```

写法与校验规则见 `references/content-schema.md` 的 `sources` 小节。

## 第二步：填槽位表，再一次并发抓完

素材**不要一格一格现想现抓**——那样一格配错，往往整轮重跑。先把口播切成「槽位表」，再交给本 skill 自带的脚本并发抓取。

### 2.1 先填 `slots.json`（这一步只填表，不动手抓）

在素材包目录里写 `slots.json`：口播里每一处需要画面来顶，就记一格。

```json
{
 "assets": [
  {"num":"01","kind":"image","site":"pexels","queries":["programming code on computer screen"],
   "summary":"显示器上密排的程序代码特写","tags":["代码","屏幕","编程"],
   "anchor":"你让它看一段代码"},
  {"num":"06","kind":"video","site":"mixkit","queries":["server|datacenter"],
   "summary":"服务器机柜指示灯闪动（视频）","tags":["机房","服务器"],
   "anchor":"在后台把代码库整个打包上传"},
  {"num":"07","kind":"image","site":"og","queries":["https://www.nsfocus.com.cn/"],
   "summary":"绿盟科技官网横幅","tags":["绿盟","官网"],
   "anchor":"信通院和绿盟完成首轮核查"}
 ]
}
```

每格六个字段：`num` 编号（决定素材顺序，跟着文案推进走）、`anchor` 从口播里**原样摘**一小句、`summary` 这格该出现什么画面（写画面，不写寓意）、`tags` 2 到 5 个中文短标签、`queries` 搜什么词（可以多给几个备选）、`site` 走哪条取法（见 2.2）。

**`queries` 只写能被字面搜到的东西。** 搜索引擎按字面匹配，抽象概念一律翻车：`饼状图` 搜出煎饼，`机械硬盘 拆开` 搜出齿轮，`云计算 数据中心` 搜出蓝天白云。换成 `donut chart`、`hard disk drive closeup`、`data center server room` 就对了。实在配不出实物的概念，这一格删掉，让它退化成文字卡——比配错画面好。

### 2.2 四种取法，按素材类型选

| `site` | 用在 | 说明 |
|---|---|---|
| `pexels` | 概念图与通用实拍（图片主力） | 图库 API，**要用用户自己的 key**；没 key 时脚本自动降级到 `bing` 并打印提示 |
| `mixkit` | B-roll 视频（视频主力） | 分类页先落一份 24 小时索引，再从索引里按关键词挑素材 ID 下载。**绝不拿正则猜文件名**——猜过一次就把「美甲锉」当成「文件」 |
| `og` | 公司、机构、产品、人物的真图 | `queries` 填**网页地址**（脚本抓该页 `og:image`），也可以**直接填那张图的地址**。官网首页常常只有几十像素的 logo，抓不到能用的图——真图一般在新闻报道页或产品页里。这是唯一能拿到「真的是它本人/它本机构」的路 |
| `bing` | 兜底 | 只在上面三条都拿不到时用，限速最严，质量最差 |

**key 从哪来**：让用户自己去 `pexels.com/api` 免费申请一把，运行时用环境变量 `PEXELS_API_KEY` 或 `--pexels-key` 传进脚本。**任何情况下都不要把 key 写进 skill、写进素材包、或者替用户去申请。** 配额是每把 key 每月 25000 次请求，一个素材包大约用掉 30 到 50 次。

### 2.3 一次抓完

```bash
python3 <skill 目录>/scripts/fetch_assets.py <素材包目录>
```

脚本负责的事：图片 12 路、视频 4 路并发（再快也只是堵在自己带宽上，实测这个档最快）；对同一站点按时间窗限速，免得把用户的 IP 送进别人黑名单；每张图入库前验魔数、验真实宽高、验最小尺寸，并按 md5 **去重**（两格用同一个词，第二格会自动换候选而不是复制一张）；视频下完直接裁成 9 秒单镜头。回执写进 `registry.json`，最后拼出一张 `contact_sheet.jpg`。

### 2.4 只补不合格的那几格

打开 `contact_sheet.jpg` 一次看完所有图，挑出不合格的，**只重跑那几格**：

```bash
python3 <skill 目录>/scripts/fetch_assets.py <素材包目录> --only 14,25
python3 <skill 目录>/scripts/fetch_assets.py <素材包目录> --report    # 看还缺哪几格
```

补之前先回 `slots.json` 把那一格的 `queries` 换成更具体的名词——同一个错词重跑三遍，还是同一张错图。

### 两类素材都要真实

**B-roll 视频**：人物、场景、产品操作、平台使用、行业画面、新闻现场，以及抽象概念对应的实拍。

**图片**优先收集：

- 相关产品、公司和人物图片
- App 真实界面与历史版本截图
- 新闻报道配图
- 行业报告与数据图表
- 产品对比图
- 活动、事件和历史资料图片
- Logo、宣传图和媒体资料图
- 能准确表达文案概念的网络照片或插图

素材要**可商用、授权清楚**：优先用有明确授权的免费来源（Pexels、Pixabay、Mixkit、Wikimedia Commons 公有领域/CC 图），每张图、每段视频的来源页与授权由脚本记进 `registry.json`，再随 `manifest.json` 的 `source_url` 和 `license` 交出去。走 `og` 或 `bing` 拿到的图，授权没确认就在 `license` 里如实写「未确认」，别替用户打包票；拿不到明确授权又非用不可的，在 `content.json` 的 `notes` 里点名是哪几格，让用户自己决定留不留。

### 明确不要的素材

- 不要原创 Motion 动效
- 不要用前端框架渲染概念动画
- 不要把 B-roll 视频抽帧后当作图片素材
- 不要用低信息量的占位图代替真实资料
- 不要为了凑数量收集大量与文案只有弱关联的素材

你交的素材就是成片的**全部画面来源**：导入素材包后，GetCut 不会再联网搜图或补空镜。所以素材要覆盖整篇文案，别让某一段找不到画面可配；同时也别为了凑数塞弱关联的东西，宁缺毋滥——真配不上时会退化成纯文字卡，那比配错画面好。

一分钟快讯通常 20 到 30 张图加 8 到 10 段视频（槽位表 30 格上下）；3 到 5 分钟的深度片按**每分钟 12 到 16 格**估，槽位表填 40 到 80 格。素材远超这个量时会沿 manifest 顺序均匀抽取，所以槽位编号的顺序必须跟着文案推进走，这一点由 `slots.json` 的排列保证。

素材方向尽量和第零步确认的成片形态一致：

- **视频**：方向不一致时，成片会按满屏裁切（`cover`）适配画布，主体靠边时容易被切掉。竖屏成片优先找竖屏视频，横屏成片优先找横屏视频。
- **图片**：一律完整展示、不裁切；方向不一致时只是上下或左右多出模糊铺底，横竖屏混用问题不大。

## 第三步：给每个视频定好用哪几秒

走 `slots.json` 的 `mixkit` 槽位抓回来的视频，脚本已经自动裁成 9 秒单镜头（取中段，避开片头），这一格可以跳过。**只有你自己手工塞进 `videos/` 的原片，才需要在这一步处理。**

成片里一个镜头只用几秒，所以别丢一段几十秒的原片过来就完事。两种做法任选：

- **自己裁好**（推荐）：直接交裁完的短片段，一个文件就是一个镜头，8 到 12 秒最好用。宁可长一点：镜头时长由配音决定，最长可能到 9 秒，片段比镜头短时只能把它慢放补足，短得太多画面就会明显发滞。

```bash
ffmpeg -ss <起点秒> -to <终点秒> -i 原视频.mp4 \
  -map 0:v:0 -c:v libx264 -pix_fmt yuv420p -preset veryfast -movflags +faststart \
  videos/01-xxx.mp4
```

- **交原片并在 manifest 里标出选段**：写上 `clip_start_seconds` / `clip_end_seconds`，GetCut 按这个窗口裁。

裁或者标之前，用 `ffprobe -v error -show_entries format=duration -of csv=p=0 原视频.mp4` 确认时长，别落在黑屏、片头台标或转场糊帧上，挑画面稳定、主体清晰的一段。

有个坑要避开：如果你交的是长原片，manifest 里又写了 `summary` 和 `tags`、却没写选段时间，GetCut 会认为你已经挑好了，直接从第 0 秒开始取——大概率取到片头。要么裁好，要么把选段时间写上，要么干脆别写 `summary` 和 `tags`，让 GetCut 自己看片挑镜头。

音轨和编码不用管：GetCut 会统一重编码成静音的 H.264 mp4，成片用自己的配音和 BGM。文件是常见的 mp4 / mov / mkv / webm 就行，带不带原声都无所谓。

## 第四步：生成 manifest.json

`manifest.json` 由脚本从第二步的回执生成，**不要手抄**——三十几格素材手抄一遍，路径与文件名极易对错：

```bash
python3 <skill 目录>/scripts/build_manifest.py <素材包目录>
```

它按 `slots.json` 的编号顺序排布素材，把每格的 `summary`、`tags`、`anchor` 落成 `summary`、`tags`、`script_anchor`，并带上抓取时记下的 `source_url` 与 `license`。图片不会写选段时间，已裁好的视频同样不会。

动手前仍要读一遍 `references/manifest-schema.md`：手工补素材、改画面描述、或者要交未裁的原片（这时才写 `clip_start_seconds` / `clip_end_seconds`），都得照那份字段说明来。`summary` 是单行中文、只描述画面里看得见的东西；`tags` 2 到 5 个中文短标签。

## 第五步：写 content.json

读 `references/content-schema.md`，按里面的模板写。

只有 `schema_version`、`title`、`script`、`hook_headline`、`sources`、`pronunciations` 和 `notes` 这几项：`schema_version` 固定填 `1`，是必填的协议版本号，漏了会直接校验失败；其余全是内容。除这几项之外的字段一律不接受，把渲染参数（包括成片形态）写进去同样会校验失败。

写之前再对一遍第零步确认的形态：竖屏信息版必须带上用户确认过的 `hook_headline`，竖屏视频与横屏视频必须没有这个字段。校验脚本不知道用户选了哪个形态，这一项对不上它查不出来，只能你自己核。

## 第六步：自校验并打包

打包前先跑自校验，它会检查字段、文件对应关系和视频选段窗口。脚本在这个技能目录的 `scripts/` 下，用绝对路径调用：

```bash
python3 <技能目录>/scripts/validate_bundle.py <素材包目录>
```

有报错就改到通过为止，不要带着报错打包。校验通过后打包：

```bash
cd <素材包目录>
zip -r ../bundle.zip content.json manifest.json images videos -x '.*' -x '__MACOSX/*'
```

环境里没有 `zip` 命令时（Windows 常见），用 Python 打包，`content.json` 必须落在 zip 根目录：

```bash
python - <<'EOF'
import zipfile
from pathlib import Path
d = Path('.').resolve()
out = d.parent / (d.name + '.zip')
with zipfile.ZipFile(out, 'w', zipfile.ZIP_DEFLATED) as z:
    for sub in ('content.json', 'manifest.json', 'images', 'videos'):
        p = d / sub
        if p.is_file():
            z.write(p, p.name)
        elif p.is_dir():
            for f in sorted(p.rglob('*')):
                if f.is_file():
                    z.write(f, f.relative_to(d).as_posix())
print(out)
EOF
```

zip 里不要放软链接，也不要放上面结构之外的其他文件。

## 交付

打包完成后，把 ZIP 的本地路径告诉用户，并明确说明：技能已经把素材包做在电脑上，下一步请用户在**自己的电脑浏览器**打开 GetCut，登录后选择这个 ZIP，核对文案与设置，再点击开始生成。

这个技能不登录 GetCut、不读取 Cookie、不索要密码或令牌、不自动上传、不创建云端任务，也不在服务器上继续渲染。画面渲染和混音由用户当前打开的 GetCut 成片台完成；用户可以在同一页面下载成片或导出剪映图层。

如果用户明确要求“帮我上传”，也只能给出上传入口和本机文件路径，由用户在浏览器里完成登录、选择文件和确认；不要代替用户点击提交。

## 交付前自查

- [ ] 对话按剧本走（候选→交稿→形态→打包），**稿子被用户点头后才配素材**；答案来自用户本次的回复，不是从记忆或历史偏好推断的；用了兜底默认值的已在回复里说明
- [ ] 候选按六维打分卡评过分，「有料」一票否决执行了——挖不出料的题没做
- [ ] 动笔前找过料（两条消息摆一起的门道 / 老手的门道），切入点三行字写死，第三行是别处没有的答案
- [ ] 全片按五段骨架走完，是一条链子不是串珠；开头钉的疑问在结尾接住了
- [ ] 稿子里有至少一处观众别处看不到的料，通稿复述没超过全片一半
- [ ] 过了不庸俗清单：无惊悚口癖、无标题党、无结尾鸡汤；金句含本片特有的事实或数字，不是通用对仗句
- [ ] 稿子过了利益三问（观众看完得到什么 / 会转给谁 / 凭什么转）；开头三句点到了观众利益
- [ ] 时长默认 3 到 5 分钟（1080 到 1800 字），出快讯是有天窗期理由的
- [ ] `scripts/validate_bundle.py` 跑通、无报错
- [ ] `content.json` 与 `manifest.json` 都写了 `"schema_version": 1`
- [ ] 素材取向与第零步确认的成片形态一致，并在 `notes` 里提醒用户在 GetCut 里选同一个形态
- [ ] `script` 是用户确认过的定稿口播，逐字可读，没有标题和舞台提示
- [ ] 形态是竖屏信息版时 `hook_headline` 存在且用户确认过；是竖屏视频或横屏视频时没有这个字段
- [ ] 每个素材都在 `manifest.json` 里有条目，`file` 路径与实际文件一一对应
- [ ] 每个视频要么已经裁成几秒的短镜头，要么在 manifest 里标了选段时间
- [ ] `summary` 都是单行中文，描述的是画面而不是含义
- [ ] 走过槽位表：`--report` 显示缺格为 0；`contact_sheet.jpg` 目检过，跑题的那几格已换词用 `--only` 补回（不是整轮重跑）
- [ ] 没有把任何 API key 写进 skill、素材包或 zip；用图库 API 时传的是用户自己的 key
- [ ] 素材都和文案有实打实的关联，没有占位图、没有抽帧图、没有渲染动画
- [ ] `content.json` 里没有渲染参数，只有内容
- [ ] `hook_headline`（如有）为 1 到 3 行、每行 12 个中文字内（英文数字按半个字算）、至少一处 `[[ ]]` 高亮
- [ ] 文案引用过公开资料时 `sources` 已默认写上，只写来源名称，不写「资料来源：」前缀，也不写免责声明
- [ ] 素材的 `source_url` 与授权都记进 manifest，可回溯
- [ ] ZIP 已在本地完成并自校验通过
- [ ] 已把 ZIP 路径和「在电脑浏览器打开 GetCut 后手动选择上传」的步骤告诉用户
- [ ] 没有读取浏览器 Cookie、密码、令牌或 API key
- [ ] 没有代替用户登录 GetCut、上传 ZIP、创建任务或触发云端渲染
